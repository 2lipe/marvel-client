describe('<Layout />', () => {
  it('should be render the heading', () => {
    // render(<Layout />);
    // expect(screen.getByRole('heading', { name: /Layout/i})).toBeInTheDocument();
  });
});

export {};
